export default function MainLP({ lineUrl }) {
  return (
    <main>
      {/* 今のLP内容をここに移す */}

      <a href={lineUrl} target="_blank" rel="noopener noreferrer">
        LINEで相談する
      </a>
    </main>
  );
}